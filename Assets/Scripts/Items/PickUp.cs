using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickUp : MonoBehaviour
{
    /// <summary>
    /// items that the player can pick up
    /// when approached, player can press a key to "grab it"
    ///     how to grab an object?
    /// </summary>

    private void GetPicked() {
        Debug.Log("picked");
    }
}
